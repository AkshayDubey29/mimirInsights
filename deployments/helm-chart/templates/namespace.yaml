{{- if .Values.namespace.create }}
apiVersion: v1Namespace
metadata:
  name: {{ .Values.namespace.name }}
  labels:
    {{- includemimir-insights.labels" . | nindent 4 }}
    app.kubernetes.io/component: namespace
{{- end }} 